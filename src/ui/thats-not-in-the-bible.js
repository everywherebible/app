import React from 'react';

export default ({what}) =>
  <div
      className="fit"
      style={{display: 'flex', alignItems: 'center', justifyContent: 'center'}}>
    <span>That's not in the Bible.</span>
  </div>
